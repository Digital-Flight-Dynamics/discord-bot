name: Heroku Deployment
on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: heroku/cli@v7.60.2
      - run: heroku git:remote -a dfd-discord-bot
      - run: git push heroku master
